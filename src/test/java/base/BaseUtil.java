package base;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;

/** This class is used to perform declares the WebDriver **/
public class BaseUtil {
    public static WebDriver driver;
}
